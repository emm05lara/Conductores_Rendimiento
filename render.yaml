services:
  - type: web
    name: dashboard-conductores
    env: python
    buildCommand: "pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"
    startCommand: "gunicorn app:server"
    plan: free
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.14"
